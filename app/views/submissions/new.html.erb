<h4 class="page-header">New submission <small><%= @problem.id %>. <%= @problem.name %></small></h4>

<%= render 'form' %>

<div class="mt-1">
  <%= link_to 'Show Problem', contest_adaptive_polymorphic_path([@problem]), class: 'btn btn-primary btn-sm' %>
  <%= link_to 'Back to Submissions List', contest_adaptive_polymorphic_path([:submissions]), class: 'btn btn-secondary btn-sm' %>
</div>
