<h4 class="page-header">Editing announcement</h4>

<%= render "form", announcement: @announcement %>

<br>

<%= link_to 'Back to Announcements', announcements_path, :class => 'btn btn-default btn-xs' %>
